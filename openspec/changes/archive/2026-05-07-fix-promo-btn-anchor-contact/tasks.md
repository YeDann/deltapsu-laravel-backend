## 1. 修改 Landing Page

- [x] 1.1 在 `landing-din-rail-infinity-ready.blade.php` 將 `<button id="promo-mini-btn" onclick="document.getElementById('promo-overlay').classList.add('is-open')" ...>` 改為 `<a href="#contact" id="promo-mini-btn" ...>`，保留 SVG icon 內容，移除 `type` 和 `onclick`

## 2. 驗證

- [x] 2.1 點擊右下角按鈕，確認頁面捲動至 `#contact` section
